div(class="h-full overflow-y-auto")
    div(class="max-w-3xl mx-auto px-8 py-8", *ngIf="data.user && service.auth.show(true)")
        //- 기본정보 카드
        div(class="bg-white border border-neutral-200 rounded-lg overflow-hidden")
            div(class="flex items-center px-5 py-4 border-b border-neutral-200")
                wiz-component-dot
                h2(class="ml-3 text-lg font-semibold text-neutral-900") 기본정보

            div(class="divide-y divide-neutral-100")
                //- 이메일
                div(class="flex items-center min-h-[48px] max-sm:flex-col max-sm:items-start")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch") 이메일
                    div(class="flex-1 p-2")
                        input(type="text", class="w-full px-3 py-2 text-sm bg-neutral-50 border border-neutral-200 rounded-md text-neutral-400 cursor-not-allowed", [(ngModel)]="data.user.email", disabled)

                //- 이름 / 연락처
                div(class="flex items-center min-h-[48px] max-sm:flex-col max-sm:items-start")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch") 이름
                    div(class="flex-1 p-2")
                        input(type="text", class="w-full px-3 py-2 text-sm border border-neutral-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-400 transition-colors", [(ngModel)]="data.user.name")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch") 연락처
                    div(class="flex-1 p-2")
                        input(type="tel", class="w-full px-3 py-2 text-sm border border-neutral-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-400 transition-colors", [(ngModel)]="data.user.mobile")

                //- 아이콘
                div(class="flex items-center min-h-[48px] max-sm:flex-col max-sm:items-start")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch") 아이콘
                    div(class="flex-1 flex items-center gap-2 p-2")
                        button(class="shrink-0 w-9 h-9 border border-neutral-200 rounded-lg bg-contain bg-center bg-cover bg-no-repeat hover:ring-2 hover:ring-blue-500/20 transition-all", [ngStyle]="{'background-image': 'url(' + data.user.profile_image + ')'}", (click)="updateIcon()", aria-label="프로필 아이콘 변경")
                        button(class="px-3 py-1.5 text-sm font-medium text-white bg-neutral-600 rounded-md hover:bg-neutral-500 transition-colors", (click)="updateIcon()") 아이콘 등록
                        button(class="px-3 py-1.5 text-sm font-medium text-blue-600 hover:underline", *ngIf="data.user.profile_image", (click)="deleteIcon()") 아이콘 삭제

            div(class="flex justify-end px-5 py-3 border-t border-neutral-100 bg-neutral-50")
                button(class="px-4 py-2 text-sm font-medium text-white bg-blue-500 rounded-md hover:bg-blue-600 transition-colors", (click)="update()") 저장하기

        //- 비밀번호 변경 카드
        div(class="bg-white border border-neutral-200 rounded-lg overflow-hidden mt-8")
            div(class="flex items-center px-5 py-4 border-b border-neutral-200")
                wiz-component-dot
                h2(class="ml-3 text-lg font-semibold text-neutral-900") 비밀번호 변경

            div(class="divide-y divide-neutral-100")
                //- 현재 비밀번호
                div(class="flex items-center min-h-[48px] max-sm:flex-col max-sm:items-start", *ngIf="data.user.hasPassword")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch") 현재 비밀번호
                    div(class="flex-1 p-2")
                        input(type="password", class="w-full px-3 py-2 text-sm border border-neutral-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-400 transition-colors", [(ngModel)]="data.password.current")

                //- 새 비밀번호
                div(class="flex items-center min-h-[48px] max-sm:flex-col max-sm:items-start")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch")
                        span(*ngIf="data.user.hasPassword") 비밀번호 변경
                        span(*ngIf="!data.user.hasPassword") 비밀번호 설정
                    div(class="flex-1 p-2")
                        input(type="password", class="w-full px-3 py-2 text-sm border border-neutral-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-400 transition-colors", [(ngModel)]="data.password.data")

                //- 비밀번호 확인
                div(class="flex items-center min-h-[48px] max-sm:flex-col max-sm:items-start")
                    div(class="w-1/6 max-sm:w-full px-4 py-2 text-sm font-medium text-neutral-500 bg-neutral-50 flex items-center h-full self-stretch") 비밀번호 확인
                    div(class="flex-1 p-2")
                        input(type="password", class="w-full px-3 py-2 text-sm border border-neutral-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-400 transition-colors", [(ngModel)]="data.password.repeat")

            div(class="flex justify-end px-5 py-3 border-t border-neutral-100 bg-neutral-50")
                button(class="px-4 py-2 text-sm font-medium text-white bg-blue-500 rounded-md hover:bg-blue-600 transition-colors", (click)="changePassword()") 변경하기
